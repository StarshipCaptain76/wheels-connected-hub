/** Club logo served from /public — never Lovable /__l5e paths */
export const LOGO_URL = "/apple-touch-icon.png";

/** Prefer full logo jpeg once uploaded to public/justwheels-logo.jpeg */
export const LOGO_URL_FULL = "/justwheels-logo.jpeg";

export function logoSrc(): string {
  return LOGO_URL;
}
